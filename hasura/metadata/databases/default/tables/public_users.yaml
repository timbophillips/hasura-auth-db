table:
  name: users
  schema: public
array_relationships:
  - name: refresh_tokens
    using:
      foreign_key_constraint_on:
        column: user
        table:
          name: refresh_tokens
          schema: public
